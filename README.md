# Curieus Model Atlas — public entry

Entry URL: https://tech.curieus.net/model-atlas/

This GitHub Pages site redirects to the currently running Curieus Model Atlas preview. The application, database, credentials and source code are not included in this repository.

The preview uses a temporary Cloudflare Tunnel and requires the development Mac and application server to remain running. Update the fixed destination in `index.html` when the preview URL changes or a permanent hosting endpoint is deployed.
